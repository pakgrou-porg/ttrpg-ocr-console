# ─────────────────────────────────────────────────────────────────────────────
# TTRPG OCR Console — Environment Variables Reference
# This file documents all required and optional environment variables.
# On the Manus platform, configure these in Settings > Secrets.
# For external deployments, set these in your hosting environment.
# NEVER commit actual secret values to git.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database ──────────────────────────────────────────────────────────────────
# MySQL-compatible connection string (TiDB or MySQL 8+)
DATABASE_URL=mysql://user:password@localhost:4000/ttrpg_ocr

# ── Session & Encryption ──────────────────────────────────────────────────────
# Secret for signing session cookies. Must be at least 32 characters.
# Generate with: openssl rand -hex 32
JWT_SECRET=<min-32-char-random-string>

# AES-256-GCM encryption key for stored API keys and credentials.
# MUST be different from JWT_SECRET. Must be at least 32 characters.
# Generate with: openssl rand -hex 32
# WARNING: Changing this key will invalidate all existing encrypted secrets.
CREDENTIAL_ENCRYPTION_KEY=<min-32-char-random-string-different-from-JWT_SECRET>

# ── Manus OAuth ───────────────────────────────────────────────────────────────
# Injected automatically by the Manus platform.
# For local development, obtain from your Manus project settings.
VITE_APP_ID=<manus-app-id>
OAUTH_SERVER_URL=https://oauth.manus.im
VITE_OAUTH_PORTAL_URL=https://manus.im/oauth/portal

# ── Manus Built-in APIs ───────────────────────────────────────────────────────
# Injected automatically by the Manus platform.
# Used for LLM inference, image generation, S3 storage, and notifications.
BUILT_IN_FORGE_API_KEY=<forge-api-key>
BUILT_IN_FORGE_API_URL=<forge-api-url>
VITE_FRONTEND_FORGE_API_KEY=<frontend-forge-api-key>
VITE_FRONTEND_FORGE_API_URL=<frontend-forge-api-url>

# ── Application ───────────────────────────────────────────────────────────────
VITE_APP_TITLE=TTRPG OCR Console
VITE_APP_LOGO=<cdn-url-to-logo-image>
OWNER_OPEN_ID=<manus-owner-open-id>
OWNER_NAME=<manus-owner-name>

# ── Analytics (optional) ──────────────────────────────────────────────────────
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# ── Server ────────────────────────────────────────────────────────────────────
# In production, the Manus platform sets PORT automatically.
PORT=3000
NODE_ENV=development
